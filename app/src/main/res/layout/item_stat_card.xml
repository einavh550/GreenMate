<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    app:cardBackgroundColor="?attr/colorSurfaceVariant"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/spacing_md">

        <ImageView
            android:id="@+id/icon_stat"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="@null"
            app:tint="@color/primary"
            tools:src="@drawable/ic_plant" />

        <TextView
            android:id="@+id/text_stat_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            android:textAppearance="@style/TextAppearance.GreenMate.Headline"
            android:textColor="@color/primary"
            tools:text="12" />

        <TextView
            android:id="@+id/text_stat_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.GreenMate.Caption"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:gravity="center"
            tools:text="Total Plants" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
